import React from 'react';
import Grid from '@mui/material/Grid';

export default function ServicesDash() {
    return (
        <Grid container direction="row" spacing={5} style={{overflow: 'hidden'}}>
        </Grid>
    );
}